{
  "name": "log-wrapper",
  "version": "0.1.1",
  "description": "Wrapper to be able to manage situation where a logger cannot exist",
  "main": "index.js",
  "scripts": {
    "lint": "node_modules/jshint/bin/jshint index.js && node_modules/jscs/bin/jscs .",
    "test": "npm run lint",
    "push": "npm test && git push && git push --tags && npm publish",
    "preversion": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Normegil/log-wrapper"
  },
  "keywords": [
    "log",
    "logging",
    "wrapper",
    "bunyan"
  ],
  "author": "Olivier Ory",
  "license": "ISC",
  "dependencies": {
    "lodash": "^3.10.1"
  },
  "devDependencies": {
    "jscs": "^2.6.0",
    "jshint": "^2.8.0"
  }
}
